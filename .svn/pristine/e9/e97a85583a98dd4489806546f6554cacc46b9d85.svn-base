// Generated code from Butter Knife. Do not modify!
package com.uyou.copenaccount.ui;

import android.view.View;
import android.webkit.WebView;
import android.widget.Button;
import androidx.annotation.CallSuper;
import androidx.annotation.UiThread;
import butterknife.Unbinder;
import butterknife.internal.Utils;
import com.uyou.copenaccount.R;
import java.lang.IllegalStateException;
import java.lang.Override;

public class NetProtocolActivity_ViewBinding implements Unbinder {
  private NetProtocolActivity target;

  @UiThread
  public NetProtocolActivity_ViewBinding(NetProtocolActivity target) {
    this(target, target.getWindow().getDecorView());
  }

  @UiThread
  public NetProtocolActivity_ViewBinding(NetProtocolActivity target, View source) {
    this.target = target;

    target.webProtocol = Utils.findRequiredViewAsType(source, R.id.web_protocol, "field 'webProtocol'", WebView.class);
    target.btn_confirm = Utils.findRequiredViewAsType(source, R.id.bus_btn_confirm, "field 'btn_confirm'", Button.class);
  }

  @Override
  @CallSuper
  public void unbind() {
    NetProtocolActivity target = this.target;
    if (target == null) throw new IllegalStateException("Bindings already cleared.");
    this.target = null;

    target.webProtocol = null;
    target.btn_confirm = null;
  }
}
